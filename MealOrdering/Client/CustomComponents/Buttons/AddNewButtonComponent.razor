@inherits BaseButtonComponent

<ButtonComponent Caption="@(Caption ?? "Add")"
                 Color="@(Color ?? "primary")"
                 IconText="@(IconText ?? "plus-square")"
                 OnClick="OnClick"
                 ToolTipStr="@(TooltipStr ?? "Add")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />
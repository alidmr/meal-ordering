@inherits BaseButtonComponent


<ButtonComponent Caption="@(Caption ?? "Delete")"
                 Color="@(Color ?? "danger")"
                 IconText="@(IconText ?? "trash-o")"
                 OnClick="OnClick"
                 ToolTipStr="@(TooltipStr ?? "Delete")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />